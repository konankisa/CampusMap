# Creates an empty graph, adds 2 node to it,
# and adds an edge between the 2 nodes

# Create a graph
CreateGraph graph

# Add a node
AddNode graph node1

# Add another node
AddNode graph node2

# Add an edge from node1 to node2 in graph
AddEdge graph node1 node2 edge