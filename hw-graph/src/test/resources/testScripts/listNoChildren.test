# Creates a graph, adds a node, and
# returns a list of the children

# Create a graph
CreateGraph graph

# Add a node to graph
AddNode graph node1

# List the children of the parent node
ListChildren graph node1