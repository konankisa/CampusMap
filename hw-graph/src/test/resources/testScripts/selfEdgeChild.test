# Creates an empty graph, adds a node to it,
# adds an edge to itself, and gets the child

# Create a graph
CreateGraph graph

# Add a node to graph
AddNode graph node

# Add an edge from node to node in graph
AddEdge graph node node edge

# Get the children of node
ListChildren graph node